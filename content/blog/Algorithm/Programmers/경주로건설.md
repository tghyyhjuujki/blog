---
title: 프로그래머스/C++) 징검다리 건너기(슬라이딩 윈도, 이분탐색)
date: 2021-01-09 21:37:57
category: Algorithm
draft: false
---

문제는 [징검다리 건너기](https://programmers.co.kr/learn/courses/30/lessons/64062)에서 확인가능하다.



```c
#include <string>
#include <vector>
#include <queue>

using namespace std;

struct CAR_STATE {
	int dir_r, dir_c, wall, cost;
};
int _size, ret = 1111111;
int dr[4] = { 0,0,1,-1 };
int dc[4] = { 1,-1,0,0 };
CAR_STATE arr[25][25];

int solution(vector<vector<int>> board) {
	int answer = 0;
	_size = board.size();
	for (int i = 0; i < _size; ++i) {
		for (int j = 0; j < _size; ++j) {
			arr[i][j].wall = board[i][j];
		}
	}
	arr[0][0].cost = 0;
	arr[0][0].wall = 1;

	queue<pair<int, int> > q;
	q.push({ 0,0 });
	while (!q.empty()) {
		int r = q.front().first;
		int c = q.front().second;
		q.pop();
		for (int i = 0; i < 4; ++i) {
			int nr = r + dr[i];
			int nc = c + dc[i];
			if (nr < 0 || nc < 0 || nr >= _size || nc >= _size)continue;
			if (arr[nr][nc].wall != 0)continue;
			if()
		}
	}
	return ret;
}
```

